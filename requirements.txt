# Core dependencies for DroneAutonomy vision pipeline
# Compatible with Windows + NVIDIA RTX 3060
# 
# IMPORTANT: Use Python 3.11 or 3.12 for full compatibility
# - Python 3.13: OpenCV wheels not available (use conda or Python 3.11/3.12)
# - Python 3.10 or older: Torch CUDA support limited

# Computer vision and video processing
opencv-python>=4.10.0
opencv-contrib-python>=4.10.0

# Deep learning and inference (CUDA-enabled)
# Install PyTorch with CUDA 11.8 support (required for GPU acceleration + compatible with CUDA 12.9):
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118 --force-reinstall
# For CPU-only: pip install torch torchvision
torch==2.7.1
torchvision==0.22.1
torchaudio==2.7.1
ultralytics>=8.0.0  # YOLO models (TensorRT-accelerated)

# TensorRT for optimized depth estimation (REQUIRED)
# Install TensorRT for Python:
# pip install tensorrt
# See: https://docs.nvidia.com/deeplearning/tensorrt/install-guide/
# Note: TensorRT Python bindings require CUDA 11.8 or 12.x
# Uses PyTorch's CUDA bindings for memory management (no need for pycuda)
tensorrt>=8.6.0  # NVIDIA TensorRT for FP16 depth inference

# Depth Anything V2 Small - TensorRT FP16 optimized (no PyTorch models needed)
# Model converted to TensorRT engine for optimal RTX 3060 Mobile performance
# ONNX model included: depth_anything_v2_vits.onnx
# Convert to TensorRT with: python scripts/convert_to_tensorrt.py

# MAVLink communication
pymavlink>=2.4.37
pyserial>=3.5  # For USB serial port detection and communication

# Numerical computing
numpy>=1.24.0
scipy>=1.10.0

# Image processing
Pillow>=10.0.0

# Configuration management
pyyaml>=6.0

# Utilities
tqdm>=4.65.0
colorama>=0.4.6

# GUI Framework (PyQt6)
PyQt6>=6.6.0  # Modern Qt6-based GUI framework
PyQt6-Qt6>=6.6.0  # Qt6 runtime libraries

# AirSim for simulation (requires special installation)
# Dependencies for AirSim
msgpack-rpc-python>=0.4.1
tornado<5,>=3
backports.ssl-match-hostname>=3.7.0
# Install AirSim with: pip install --no-build-isolation airsim
# Note: Must be installed after numpy and other dependencies are already installed
airsim>=1.8.1

# Optional: TensorRT for optimized inference on NVIDIA GPUs
# Install separately: https://docs.nvidia.com/deeplearning/tensorrt/install-guide/index.html
# tensorrt

# Optional: DeepStream SDK for production deployment
# Install separately: https://developer.nvidia.com/deepstream-sdk
